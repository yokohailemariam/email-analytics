import GoogleLogin from "@/components/LoginWithGmail";

const Auth = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <GoogleLogin />
    </div>
  );
};

export default Auth;
